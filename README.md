# Preflight

A pre-sign transaction firewall. Decodes what a wallet approval actually does in plain English before you sign it, checks it against on-chain approval history via The Graph, and computes a verdict inside a Chainlink CRE confidential workflow.

Built for ETHOnline 2026.

## Status

Early scaffolding — see commit history for progress.
