{
  "name": "preflight-subgraph",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ preflight-approvals"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.38.2"
  }
}
